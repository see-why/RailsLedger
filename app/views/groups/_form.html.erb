<%= form_with(model: group, class: "record-form", id: "group-form") do |form| %>
  <% if group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
        <% group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name, class: "text-box first last", placeholder: "Name" %>
  </div>

  <div class="category-box">
    <%= form.label :icon, class: "unit-label", style: "display: block" %>
    <%= form.file_field :icon %>
  </div>
  
<% end %>
