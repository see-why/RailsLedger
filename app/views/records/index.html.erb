<p style="color: green"><%= notice %></p>

<h1>Records</h1>

<div id="records">
  <% @records.each do |entity| %>
    <%= render record %>
    <p>
      <%= link_to "Show this Record", entity %>
    </p>
  <% end %>
</div>

<%= link_to "New Record", new_entity_path %>
